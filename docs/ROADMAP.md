# 개발 로드맵 (Claude Code 작업 단위)

## 수업 일정·평가 (SW 설계 기초 팀 프로젝트 안내 PDF 요약)
- 기획 발표 **10/6·10/8 (6주차)** · 요구사항 명세서 **7주차** (기능 필수, 비기능 선택; 양식: 고유번호/요구명칭/분류/수준/세부내용) · 중간점검 **11/17·11/19 (12주차)** · 데모 영상+소스 **15주차** · 최종 발표·보고서 **12/15·12/17 (16주차)**
- 평가 상: ① 기능 완성도·**버그 없음** ② 창의성/차별성 ③ 조작 편의·재미·보상 피드백 / 중: 협업, 밸런싱·플레이타임 / 하: 확장성
- **순수 시각화 요소는 평가 미반영** → 그래픽은 최소, 기능·안정성 우선
- 권장: **약 3 스테이지, 15~20분 플레이**, 5분 안에 모든 요소 시연 가능 → 데모용 치트/빠른 진행 모드 필요(7단계)
- 라이브러리·에셋·AI 사용 가능, **레퍼런스 명시 필수** → `docs/REFERENCES.md`

각 단계는 **한 세션 = 한 단계**로 끊는다. 화면 작업 시 `docs/UI_REFERENCE.md`(참고 이미지 요약)를 본다. 새 세션은 `CLAUDE.md` → 이 파일 순으로만 읽고 시작한다(GDD 전체 읽기 금지).

| 단계 | 내용 | 추천 모델 | 상태 |
|---|---|---|---|
| 1 | 코어 규칙 엔진(순수 C#): 전투·정화·배고픔·스킬 12종·유물 18종·스테이지/노드/포식·집보내기·조각/원석 + dotnet 검증 | Opus (중간) | ✅ 완료 |
| 2 | Unity 연결층: `GameManager`(RunState 보유) + **코드로 생성하는 UI**(씬/프리팹 YAML 편집 최소화). 화면: 파티 선택 → 유물 장착 → 노드 맵 → 사이드뷰 전투 → 전투 후(먹기/휴식) → 이벤트 → 작별 → 결과. 도형+텍스트 플레이스홀더 그래픽 | Sonnet 5 (높음) | ✅ 완료 — 실기 확인(종 선택~전투 진입 클릭 확인) |
| 3 | 전투 연출: 이벤트 재생 큐(피격 흔들림, 데미지 숫자, HP바 트윈), 턴 순서 바, 정화 확률 표시(§17) | Sonnet 5 (높음) | ✅ 완료 — 실기 확인 |
| 4 | 필드 탑뷰: 노드 사이를 걷는 탑뷰 맵(타일맵), 지형별 배경 4종 | Sonnet 5 (높음) | |
| 5 | 적 AI 고도화(적도 종별 스킬 사용), 이벤트 추가, 원석 용도(§20-8) 확정 후 시너지 스킬 | Opus (중간) — 규칙 설계가 섞임 | |
| 6 | 밸런스: `Tests/CoreSim` 봇 개선 + `Balance.cs` 튜닝, 실제 플레이테스트 반영 | Sonnet 5 (높음) | |
| 7 | 데모 모드(5분 시연용 치트/스킵), 아트/사운드 교체, 타이틀·인트로, 빌드 | Sonnet 5 (높음) | |

## 2단계 결과 (Unity 연결층) — ✅ 실기에서 확인됨
Unity 6.3.22f1에서 실제로 열어 종 선택 → 유물 장착 → 노드 맵 → 전투 화면까지 클릭 확인했다.
빌드 중 잡은 문제 2건(둘 다 수정 완료, 커밋 히스토리 참고):
- `RelicEquipScreen`: 람다 안에서 void 메서드 두 개를 삼항연산자로 고르려다 CS0201 → if/else로 수정
- `UIFactory.ScrollList`: 뷰포트 이미지를 완전 투명(alpha 0)으로 만들었더니 Mask가 내용까지 통째로 가려버림
  (Unity Mask는 알파로 자르기 때문에 alpha=1 + showMaskGraphic=false로 써야 함) → 동물 선택 목록이 안 보이던 원인

- 구조: `Bootstrap.cs`(카메라·EventSystem·Canvas를 코드로 생성, 씬 파일은 완전히 비워둠) →
  `GameManager`(RunState 보유, 모든 상태 변경 메서드 제공) → `ScreenRouter`(Phase별로 화면 전체를 다시 그림) →
  `UI/Screens/*`(화면 8개: 종 선택/유물 장착/맵/전투/전투 후/이벤트/작별/결과) → `UI/UIFactory`(코드로 uGUI 조립, §2 지침).
- 한글: 레거시 `UnityEngine.UI.Text` + 나눔고딕(`Assets/Resources/Fonts`, 코드에서 `Resources.Load<Font>`). TextMeshPro는 사용하지 않음(한글 SDF 아틀라스 세팅 없이 더 단순함).
- 그래픽: 색 배경(Image)과 텍스트뿐, 스프라이트 없음(ROADMAP 상단 평가 기준 참고).
- **다음에 할 일(사람)**: Unity에서 빈 씬 하나 만들어 저장(`Assets/Scenes/Main.unity`) → 열어서 Play 버튼 → Console 확인.
  에러가 있으면 그 내용을 그대로 다음 세션에 붙여넣을 것. 특히 `FedAndFound.Game.asmdef`의 `UnityEngine.UI` 참조가
  Unity 6.3의 실제 어셈블리 이름과 다르면(EventSystem 관련 타입을 못 찾는 CS0246) asmdef 인스펙터에서 참조를 추가해야 한다.

## 3단계 결과 (전투 연출) — ✅ 실기에서 확인됨
턴 순서 바·정화 확률 표시는 2단계에서 이미 있었음. 이번엔 아래를 추가:
- `GameManager.PlayEvents`: 전투 한 턴의 이벤트를 한 줄씩 로그에 얹으며 0.22초씩 멈춤(코루틴). 재생 중엔
  `Animating=true`가 되어 `BattleScreen`이 행동 버튼을 숨기고 "…"만 보여줌(중복 입력 방지).
- `EventFeedOverlay`: 화면 전체를 다시 그려도 안 지워지는 별도 오버레이. 이벤트 종류(피격/치유/정화/기절)에
  따라 화면을 짧게 색으로 물들임(적 피격=초록, 아군 피격=빨강, 정화=파랑, 기절=진한 빨강).
- 전투 로그가 항상 최신 줄로 자동 스크롤되도록 `UIFactory.ScrollToBottom` 추가.
- **의도적으로 안 한 것**: HP바가 프레임 단위로 부드럽게 깎이는 애니메이션. Core 전투 엔진이 한 턴을
  통째로 계산해서 결과만 주는 구조라, 중간값을 보여주려면 Core를 바꿔야 해서 비용 대비 효과가 낮다고 판단.
- **다음에 할 일(사람)**: Unity Pull → Play → 전투에서 공격/스킬 한 번 눌러서 로그가 한 줄씩 올라오고
  화면이 짧게 색으로 번쩍이는지, 그 동안 버튼이 잠기는지 확인. 에러 있으면 그대로 붙여넣기.

## 현재 알려진 이슈 (1단계 결과)
- 무작위 봇 기준 승률 약 5%. 봇이 방어·유물 판단을 안 하므로 실제 체감 난이도는 더 낮을 것. 3스테이지(1마리 단독)에서 가장 많이 막힌다 → 6단계에서 조정.
- 봇 기준 정화가 물리치기보다 많이 선택됨 → §0 "절대 우위 금지" 원칙상 `CurseGaugeMax`, 보상량 조정 필요.
- 해석이 필요했던 부분(구현 기준):
  - 매복: 사용한 턴이 "쉬는 턴", 다음 첫 공격(기본공격 또는 피해 스킬) ×2.6
  - 여우 눈치: 대상이 이번 라운드 행동 전이면 즉시 2칸 앞당김, 이미 행동했으면 다음 라운드에 적용
  - 유지형: 라운드마다 1회 지불(켜는 순간 미지불이면 즉시 지불)
  - 마지막 불씨: 조건 충족 시 자동 발동(선택 UI 없음)
  - 0스테이지: 파티 3+사슴이 약한 보스 1마리와 바로 싸움(노드 없음)
  - 보스 HP는 남은 파티 수에 비례해 줄어듦(3→2→1마리 구조 대응)

## Unity 프로젝트 준비 (완료)
- `Packages/manifest.json`, `ProjectSettings/ProjectVersion.txt`, 한글 폰트 `Assets/Fonts/NanumGothic*.ttf`는 저장소에 포함됨. Unity가 처음 열 때 나머지 ProjectSettings와 `.meta` 파일을 생성한다 → 생성된 파일은 커밋할 것.
- 렌더 파이프라인: 기본(Built-in) 2D. URP 불필요. TMP 폰트 에셋은 2단계에서 코드로 런타임 생성(`TMP_FontAsset.CreateFontAsset`).
